[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rimworldforge"
version = "0.2.0"
description = "Local-first AI modding workbench for RimWorld 1.6"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "RimWorldForge contributors"}]
license = {text = "MIT"}
dependencies = []

[project.scripts]
rwforge = "rwforge.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
